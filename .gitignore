.gradle/
build/
*/build/
local.properties
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/
.android-sdk/

# Секреты подписи — никогда не коммитим
keystore.properties
*.keystore
*.jks
release.keystore

# Артефакты ветки main (Telegram-бот на Python) живут в этом же репозитории.
# При переключении веток они остаются untracked в рабочем дереве — без этих
# правил `git add -A` затягивает их в коммит приложения (инцидент 2026-07-20:
# в 0.6.4 уехали __pycache__/*.pyc от bot smoke-тестов).
__pycache__/
*.pyc
.env
bot.db
bot.db-wal
bot.db-shm
